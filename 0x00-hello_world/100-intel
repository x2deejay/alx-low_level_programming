#include <stdio.h>

/**
*main - assembles and outputs
*
*Decription:  generates the assembly code (Intel syntax) 
*of a C code and save it in an output file.
*Return: 0
*/
int main(void)
{
main.s=gcc -s $CFILE; 
return (0);
}
