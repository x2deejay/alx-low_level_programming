#include <stdio.h>

/**
 * main - with proper grammar, but the outcome is a piece of art
 *
 * Decription:Prints with proper grammar, but the outcome is a piece of art
 * Return:0
 */
int main(void)
{
char z[100] = "with proper grammer, but the outcome is a piece of art,";
printf("%s", z); 
return (0);
}
