#include "main.h"

/**
 * print_buffer - c function that prints the content of an 
 * inputted number of bytes from the buffer;
 * prints 10 bytes per line.
 * Starts with the position of the first byte in hexadecimal (0 chars),
 * starting with '0';
